<li>
    <article class="bandeau-articles">
        <div class="text-bandeau-articles">
            <h2>{{ post.data.title }}</h2>
            <small>Article de {{ post.data.author }}, Posté le {{ post.date | postDate }}</small>
            <p>{{ post.data.description }}</p>
            <a href="{{ post.url }}">Lire l'article</a>
        </div>
        <img src="{{ post.data.image }}" alt="{{ post.data.imageAlt }}" width="325"></article>
</li>
<style>
    .bandeau-articles {
        margin: 4rem auto;
        display: flex;
        justify-content: space-between;
        gap: 1rem;
        max-width: 1200px;
        min-height: 200px;
        overflow: hidden;
        border-radius: 0.2rem;
    }
    .text-bandeau-articles p {
        margin-bottom: 3rem;
    }
    .text-bandeau-articles {
        position: relative;
        padding: 1rem;
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
    }
    .text-bandeau-articles a {
        position: absolute;
        bottom: 1rem;
    }
    @media(max-width:900px) {
        .bandeau-articles {
            flex-direction: column-reverse;
            gap: 0;
        }
        .bandeau-articles img {
            width: 100%;
            height: 100px;
        }
        .text-bandeau-articles p {
            margin-bottom: 5rem;
        }
    }
</style>